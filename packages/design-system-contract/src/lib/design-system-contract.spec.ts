import { designSystemContract } from './design-system-contract';

describe('designSystemContract', () => {
  it('should work', () => {
    expect(designSystemContract()).toEqual('design-system-contract');
  });
});
