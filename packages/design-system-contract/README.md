# design-system-contract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build design-system-contract` to build the library.

## Running unit tests

Run `nx test design-system-contract` to execute the unit tests via [Vitest](https://vitest.dev/).
