// @firefly/schema-types — DSL type definitions
// Barrels will be populated in subsequent STEPs

/** Placeholder type — replaced in STEP-1.3-002+ with actual DSL types. */
export type SchemaTypesPlaceholder = Record<string, never>;
